{% extends 'base.html.twig' %}

{% block title %}Modifier ville
{% endblock %}

{% block body %}

	{# <div class="container">
		<div class="titre">
			<h1 class="row justify-content-center">Modifier une ville</h1>
		</div>

		{{ form_start(villeForm) }}
		<div class="form-ville row justify-content-center">
			<div
				class="col-5">
				<table class="table table-striped table-sm caption-top col-sm-5" id="listeInscrit">
					<caption>Liste des participants :</caption>
					<thead>
						<tr class="row">
							<th class="col-sm-2">Pseudo</th>
							<th class="col-sm-3">Nom</th>
						</tr>
					</thead>
					{% if inscrip %}
						{% for i in inscrip %}
							<tr class="row">
								<td class="col-sm-2">ddddd</td>
								<td class="col-sm-3">dddddd</td>
							</tr>
						{% endfor %}
					{% endif %}
				</table>
				<div class="col-sm-12 text-center">
					<button class="btn btn-primary" type="submit" name="modifierSortie">Modifier</button>
					<a class="btn btn-secondary" href="{{ path('accueil') }}" title="annuler" role="button">Annuler</a>
				</div>


			</div>
		</div>
		{{ form_end(villeForm) }}
	</div> #}
{% endblock %}
