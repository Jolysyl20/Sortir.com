{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
<style>
form{display: contents;
}
</style>
<div class="container" style="margin-top:3%">

 <div class="row justify-content-center" style="border: solid 1px;
border-radius: 12px; padding:3%">
<h1 style="text-align:center">Créer un Profil</h1>
 {{ form_start(registrationForm)}}
 
    <div class="col-5 px-4">
        {{ form_row(registrationForm.pseudo)}}
        {{ form_row(registrationForm.nom)}}
        {{ form_row(registrationForm.prenom)}}
        {{ form_row(registrationForm.password)}}


    </div>
    <div class="col-5 px-4">
        {{ form_row(registrationForm.mail)}}
        {{ form_row(registrationForm.telephone)}}
        {{ form_row(registrationForm.imageFile)}}
        {{ form_row(registrationForm.roles)}}
        {{ form_row(registrationForm.actif)}}
        {{ form_row(registrationForm.site_participant)}}
        <button type="submit" style="float:right; margin-top:7%;" class="btn btn-success" >Enregistrer</button>
    </div>
  </div>

    {{ form_end(registrationForm) }}
</div>    
{% endblock %}
